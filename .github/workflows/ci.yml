name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  mac-app:
    name: macOS app (${{ matrix.scheme }}, ${{ matrix.configuration }})
    runs-on: macos-latest
    strategy:
      matrix:
        scheme: [screencast, screencast-standalone]
        configuration: [Debug, Release]
    steps:
      - uses: actions/checkout@v7
      - name: Build unsigned app
        run: |
          xcodebuild \
            -project screencast.xcodeproj \
            -scheme "${{ matrix.scheme }}" \
            -configuration "${{ matrix.configuration }}" \
            -destination 'platform=macOS' \
            CODE_SIGNING_ALLOWED=NO \
            build
      - name: Static analysis
        if: matrix.configuration == 'Release'
        run: |
          xcodebuild \
            -project screencast.xcodeproj \
            -scheme "${{ matrix.scheme }}" \
            -configuration Release \
            -destination 'platform=macOS' \
            CODE_SIGNING_ALLOWED=NO \
            analyze

  mac-app-required:
    name: macOS app
    needs: mac-app
    runs-on: ubuntu-latest
    steps:
      - run: echo "Debug and Release macOS checks passed."

  worker:
    name: Worker
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: worker
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: worker/package-lock.json
      - run: npm ci
      - run: npm --prefix ../checkout ci
      - run: npm run build:checkout
      - run: npm run check
      - run: npm test
      - run: npm run smoke
      - run: npm audit --audit-level=high
      - run: npm run build:site

  checkout:
    name: Checkout and private releases
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: checkout
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: checkout/package-lock.json
      - run: npm ci
      - run: npm test
      - run: npm audit --audit-level=high
      - run: npm run build
